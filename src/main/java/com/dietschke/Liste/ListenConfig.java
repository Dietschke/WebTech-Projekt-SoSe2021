package com.dietschke.Liste;
/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class ListenConfig {

    @Autowired
    public ListeRepository listeRepository;

    @Bean
    CommandLineRunner ListenConfig(ListeRepository listeRepository) {
        return args -> {
            Liste liste1 = new Liste(1L, "-", "-");
            Liste liste2 = new Liste(2L, "-", "-");
            Liste liste3 = new Liste(3L, "-", "-");
            Liste liste4 = new Liste(4L, "-", "-");
            Liste liste5 = new Liste(5L, "-", "-");
            listeRepository.saveAll(
                    List.of(liste1, liste2, liste3, liste4, liste5));

        };
    }
}
*/